# Trivy Configuration
# Documentation: https://aquasecurity.github.io/trivy/latest/docs/configuration/
# Note: Secret scanning is handled by Gitleaks - Trivy focuses on vulnerabilities and misconfigurations

# Scan settings
scan:
  # Security checks to enable (removed 'secret' - handled by Gitleaks)
  security-checks: "vuln,config"

  # Severity levels to report
  severity: "CRITICAL,HIGH,MEDIUM,LOW"

  # Scan all layers (removed 'secret' scanner)
  scanners: "vuln,misconfig"

# Vulnerability settings
vulnerability:
  # Type of vulnerabilities to detect
  type: "os,library"

# Note: Secret scanning removed - handled comprehensively by Gitleaks
# secret:
#   config: .trivy-secret.yaml

# Ignore unfixed vulnerabilities (set to false for demo to show all)
ignore-unfixed: false

# Timeout settings
timeout: "5m"

# Exit code
# exit-code: 1  # Uncomment to fail build on findings

# Cache settings
cache:
  # Cache backend
  backend: "fs"

# Report format
format: "sarif"

# Paths to skip
skip-dirs:
  - node_modules
  - .git
  - .github
  - test
  - tests
  - coverage

# Skip files
skip-files:
  - "**/*.md"
  - "**/*.txt"
  - "**/LICENSE"

# Database
db:
  # Skip database update (use cached)
  skip-update: false

# Output
output: "trivy-results.sarif"

# Offline mode (set to false to get latest vulnerability data)
offline-scan: false
